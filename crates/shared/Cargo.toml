[package]
name = "owl-shared"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "共用基建:signal 依赖追踪(re-export)+ testkit 算子对拍框架"

[dependencies]
owl-cuda = { path = "../backends/cuda" }
owl-iface = { path = "../backends/iface" }
